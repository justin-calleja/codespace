{
  "name": "@justinc/codespace",
  "version": "1.0.0",
  "license": "MIT",
  "bin": {
    "codespace": "./build/cli.js"
  },
  "scripts": {
    "build": "run-s clean compile && chmod +x ./build/cli.js",
    "clean": "rimraf build/*",
    "compile": "tsc -p tsconfig.json",
    "clean-start": "run-s clean start",
    "start": "npm run compile -- -w"
  },
  "dependencies": {
    "argv-cmd-lib": "^1.0.2",
    "chalk": "^4.1.1",
    "minimist": "^1.2.5",
    "minimist-options": "^4.1.0"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.1",
    "@types/node": "^15.0.2",
    "ejs": "^3.1.6",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
